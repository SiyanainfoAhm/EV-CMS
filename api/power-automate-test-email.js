/**
 * Vercel serverless proxy → Power Automate HTTP trigger.
 * Set POWER_AUTOMATE_EMAIL_URL in Vercel env (full HTTP POST URL from the flow).
 */
export { default } from "./power-automate-email.js";
